### [V51] - 2026-05-31 更新日誌

#### 系統修復與資源擴充（P0）
- **[修復] Tailscale Funnel 連線失效問題**：排查發現 PVE 宿主機上的 socat 連接埠轉發進程未運行，導致外部存取中斷。已重新啟動 socat 背景轉發進程並重啟 Funnel，恢復存取。
- **[修復] 靜態 JavaScript 快取導致介面未更新問題**：於後端 `main.py` 新增專屬 `/static/js/gigs.js` 的無快取路由並注入 `Cache-Control: no-cache, no-store, must-revalidate` 標頭，強制瀏覽器載入最新版接案中樞邏輯，徹底解決客戶端載入舊版快取而未顯示「拒絕承接」紅鈕的異常。
- **[新增] 伺服器 RAM 記憶體擴充**：將 Docker 虛擬機（VM 100）的分配記憶體從 8 GB 擴充至 16 GB，並完成硬重啟以套用配置，解決硬體資源不足問題。
- **[新增] Minecraft DDNS 自動更新**：後端實作背景更新線程，定時偵測公網 WAN IP。當 IP 變更時，自動向 Dynu DNS API 發送同步請求以更新網域記錄。

#### 虛擬化中心升級與 VM 用戶管理（P1）
- **[重構] 取消虛擬機快速部署**：移除 virtual.html 原有的快速部署 Win11/Ubuntu 等卡片。
- **[新增] 虛擬機專屬帳號管理**：在資料庫建立 VMAccount 模型，支援新增與刪除 VM 登入帳號。
- **[新增] 主控台連線與 credentials 選擇**：當用戶欲開啟某台 VM 時，系統會彈出 Modal 供其選擇登入帳號，選擇後自動於新分頁以 PVE noVNC URL 開啟 VM 控制介面。

#### 新增發案平台、開源平台與意見反饋平台（P1）
- **[新增] 發案接案委託平台**：實作 Gig 資料庫模型與對應 API。接案平台已成功移至介紹頁，建構為獨立的對外公開介面，未登入訪客亦可直接瀏覽委託案件，並實作寫入操作時的登入攔截引導。
- **[新增] 拒絕承接案件與填寫理由**：後端實作 `POST /api/gigs/{id}/reject` 端點，並在 `Gig` 模型中加入 `reject_reason` 欄位，支援自動在啟動時執行 SQLite 結構遷移。前端串接此接口，為非發案人提供「拒絕承接」紅鈕，點擊以 `prompt` 彈窗供接案人填寫拒絕理由，提交後案件狀態變更為 `Rejected`（已拒絕），且發佈者在登入後能直接在卡片上看見拒絕原因。
- **[新增] 開源代碼同步展示**：串接 GitHub API 自動同步 deven951130 的公開倉庫，於前端展示並提供一鍵複製 clone 指令的功能。
- **[新增] 意見反饋服務**：實作 Feedback 提送與處置 API，支援用戶提交反饋、管理員回覆並處置，並依 Pending/Resolved 顯示彩色狀態。

#### 視覺與 UI 體驗高級化（P2）
- **[新增] 深色科技背景圖片與動態環境浮光**：生成高畫質幾何科技深色背景 `tech_bg.png`，並於 `index.css` 引入作為 Dark Mode 背景；設計 `::before` / `::after` 環境雙色慢速漂浮光暈（Radial-gradient 與 floatGlow 動畫），營造沉浸式科技質感，並於 Light Mode 自動重置/隱藏以維持可讀性。
- **[新增] 階梯瀑布式淡入與選單微動效**：實作 `fadeInUp` 關鍵影格與階梯式延遲動畫（Staggered Delay），使各功能頁面的卡片與統計圖表在切換時呈現自然流暢的瀑布進場效果；同時為側邊欄導覽項目（`nav-item`）新增 Hover 右滑與發光微動效。
- **[修復] 深色背景圖片遭不透明容器遮擋**：修復 `.main-content` 及介紹/定價頁子區塊（如 `.pricing-reviews`、`.intro-features` 等）因帶有不透明背景色而遮蔽底層 `body` 背景圖的問題。已於 `index.css` 追加覆寫，在深色模式下將此類容器背景設為 `transparent !important`，使背景圖案與浮光順利顯現。
- **[新增] 高級科技美感升級與細節精緻化**：引入 Google Fonts 高級字型 `Outfit` 與 `Plus Jakarta Sans` 提升全站現代感；定價切換 Tab 重構為無縫膠囊軌道；為主要按鈕新增 Hover 流光白影特效（Shimmer）；方案卡片導入彩虹漸變頂線與選取發光；評語區分隔光柱與對照表（Compare Table）圓角化，並對年計費方案實作直行淡藍微光高亮。
- **[優化] 公司介紹頁面框架**：重構 intro.html，將 Landing Page 擴充為結構完整的公司介紹框架，拔除所有 Emoji 以符合排版規範。
- **[優化] 視覺設計高級化**：於 index.css 全局追加毛玻璃玻璃擬態（glassmorphism）樣式，優化按鈕縮放與輸入框聚焦外發光等微動畫，提升系統的視覺質感。

### [V50] - 2026-05-28 更新日誌

#### 登入狀態下產品介紹頁面空白修復（P0）
- **[修復] /introduce 頁面全黑異常**：為 `app.js` 中的 `loadComponent` 版本號解析機制加上 `try-catch` 保護並傳入 `window.location.origin` 作為 `base` URL，防止瀏覽器在處理相對路徑時拋出 `TypeError: Invalid URL` 錯誤導致應用程式加載中斷、`App.boot()` 無法執行而卡在全黑遮罩狀態。

### [V49] - 2026-05-28 更新日誌

#### 瀏覽器快取控制防禦與 SPA 版本同步（P0）
- **[修復] 登入後白屏與快取未更新 Bug**：解決使用者於登入後或更新部署後需手動刷新快取才能正常顯示頁面的問題。
- **[新增] 全功能 HTTP 快取控制標頭**：於後端 `main.py` 之 `home()` 回傳 `index.html` 時，注入 `Cache-Control: no-cache, no-store, must-revalidate`、`Pragma: no-cache`、`Expires: 0` 標頭，強制瀏覽器每次均請求最新的 HTML 範本，自動使舊版 JS/CSS 快取失效。

### [V48] - 2026-05-28 更新日誌

#### Proxmox 連線修復與 Docker 環境變數對位（P0）
- **[修復] Proxmox Host 連線失敗**：解決進入「虛擬化中心」時出現 `Could not connect to Proxmox host` 的 Bug。原因為 VM 的 `.env` 檔案缺失了 PVE、Blynk IoT 及 JWT Secret 等變數的宣告，且 `docker-compose.yml` 亦缺少將這些變數從宿主機轉入容器內部的 environment 對應。
- **[新增] Docker Compose 環境變數轉送**：於 `docker-compose.yml` 中補齊 PVE、Blynk、JWT、ADMIN 等變數的環境傳遞配置，透過 `docker compose --force-recreate` 重新載入，徹底解決容器因缺少密鑰導致外部連線中斷之問題。

### [V47] - 2026-05-28 更新日誌

#### 實體硬碟溫度隱藏與系統空間數據修正（P0）
- **[修復] 實體硬碟卡片溫度移除**：隱藏實體硬碟健康報告卡片中的「溫度」顯示（解決因虛擬機 QEMU 無法取得真實物理硬碟溫度而顯示 0°C 的問題）。
- **[修復] 系統儲存空間配額數據異常**：
  - **系統 SSD 擴展**：對 VM 根目錄 `/` 所在的 LVM 卷進行在線擴容，將容量由 `49G` 安全擴充至真正物理限制 `98G`，使 Dashboard 的「SSD 系統硬碟」顯示正確的容量。
  - **雲端 HDD 掛載與持久化**：重新掛載掉線的 5.5T HDD 實體分割區 `/dev/sdb1` 至專案的 `nas` 資料夾，並寫入 `/etc/fstab` 持久化，修正雲端資料碟在 Dashboard 上錯誤顯示為 `48G` 的問題。

### [V46] - 2026-05-28 更新日誌

#### 路由映射修補與獨立產品頁面優化（P0）
- **[修復] 核心助手重新整理 Bug**：將 `/axai` 路由正確映射至 `ai` 虛擬視圖，修復重新整理時無法觸發標題/圖示替換且左側選單錯誤高亮至「智慧宅控」的缺陷。
- **[新增] 登入介面專屬路徑（/login）**：前後端新增 `/login` 路由映射。未登入狀態下訪問 `/login` 將於 `/introduce` 底層上疊加登入彈窗；已登入狀態下訪問則自動重定向至 `/main`。
- **[優化] 登入彈窗關閉與跳轉機制**：關閉登入彈窗時，若當前網址為 `/login`，網址將自動退回 `/introduce`；已登入使用者在介紹頁點擊登入相關按鈕時，將直接重導向至後台主頁，避免重複登入。
- **[優化] 獨立產品介紹與定價頁面**：新增 `.full-screen-view` 全螢幕樣式，當切換至 `/introduce` 或 `/price` 時，無論登入狀態如何，皆會強制隱藏側邊欄與頂部 Header，使其呈現為完全獨立的宣傳網頁。

### [V45] - 2026-05-28 更新日誌

#### 巢狀網頁路徑路由與 History API 整合（P0）
- **[新增] HTML5 History API 巢狀路徑路由**：捨棄傳統的本機純狀態視圖切換，將全站改為標準路徑路由。支援路徑如下：
  - 未登入：`/introduce`（介紹頁）、`/price`（定價頁）。
  - 已登入：`/main`（主頁）、`/virtal`（虛擬化中心）、`/iot`（智慧）、`/axcloud`（私有雲）、`/nas`（NAS 管理中樞）、`/axai`（核心助手）、`/livedata`（實時數據）、`/system`（系統設定）、`/multiverse`（多重宇宙）。
  - 管理員：`/idmanage`（帳號管理）。
- **[新增] 後端多路徑重定向映射**：於 `app/main.py` 將上述所有前端 SPA 視圖路徑全部映射至同一個 `home()` 端點，回傳 `index.html` 模版，確保使用者在各子路徑下重新整理網頁時（Refresh）不發生 HTTP 404 錯誤。
- **[新增] 瀏覽器歷史歷程同步（popstate）**：於 `static/js/ui.js` 實作網址與視圖同步機制。點擊選單切換時利用 `history.pushState` 自動修改瀏覽器網址列；監聽 `popstate` 事件，使瀏覽器的「上一頁」與「下一頁」前進後退能無縫切換對應視圖。
- **[安全] 後台路由存取防禦（Route Guard）**：重構 `app.js` 的 `boot()` 啟動程序，未登入狀態下若直接存取後台路徑（如 `/main` 或 `/nas`），將會自動重寫至 `/introduce` 並彈出登入框；已登入的普通使用者若嘗試越權存取 `/idmanage`，系統亦會強制引導回 `/main` 視圖，確保系統安全性。

### [V44] - 2026-05-28 更新日誌

#### 安全性與隱私修補（P0）
- **[安全] 系統日誌敏感資訊遮罩**：於 `app/routers/system.py` 中實作日誌遮罩功能，非 `Administrator` 角色檢視日誌時，將自動對使用者姓名、個資及詳細檔名進行遮罩，避免個資洩漏。
- **[安全] Minecraft IP 遮蔽**：修改 `app/routers/system.py` 與 `app/routers/minecraft.py` 的 API 端點。非 `Administrator` 角色在戰情總覽與多重宇宙面板上將看到遮蔽後的外部 IP 地址（如 `1.168.**.***`）。
- **[安全] 系統廣播權限限制**：後端 `/api/system/message` 新增管理員角色驗證，非 `Administrator` 無法成功發送廣播（回傳 403）；前端亦對非管理員使用者隱藏廣播輸入區塊，防止越權呼叫。
- **[安全] 前端路由守衛（Route Guard）強化**：於 `static/js/ui.js` 實作更嚴格的未登入驗證阻攔，若 `localStorage` 內無有效 Token 且欲切換至後台視圖，系統將強制阻斷並導向 `intro` 介紹頁並彈出登入 Overlay，杜絕透過 F12 繞過蓋板檢視敏感數據。

#### 功能與 UI/UX 優化（P1）
- **[優化] 磁碟容量計算修正**：將 HDD 雲端資料碟（NAS）的空間使用量，由原先計算整塊分割區磁碟使用量（導致與 SSD 系統碟數據完全重複）改為計算 `NAS_ROOT` 目錄下所有使用者實際檔案大小的總和，實現真實的私有雲儲存佔用統計。
- **[優化] 系統日誌關鍵字篩選與分類樣式**：於 System Logs 面板新增實時「關鍵字篩選」輸入框。日誌區塊內依據事件類型（如安全警報、廣播、MC 指令、私有雲、系統等）顯示自訂底色標籤與彩色標示，提升資訊易讀性。
- **[優化] 廣播發送防呆機制**：前端廣播按鈕點擊後新增二次確認彈窗，防止管理員誤觸發送。

#### 行動端響應式設計（RWD）（P2）
- **[新增] 行動端漢堡選單（Hamburger Menu）**：針對 `768px` 以下的行動裝置與平板，頂部 Header 新增「☰」選單按鈕，點擊可滑入側邊導覽列抽屜；點擊導覽項目或側邊欄外部時，抽屜將自動收合。
- **[優化] 佈局自適應**：在 `static/css/index.css` 中加入行動端覆寫規則，將側邊欄改為懸浮滑入式抽屜，主內容區邊距歸零，戰情卡片網格與日誌/硬碟卡片在小螢幕下改為雙欄或垂直單欄堆疊，徹底解決行動裝置畫面擠壓與破版問題。

### [V43] - 2026-05-27 更新日誌
- **[新增] 定價頁面（Pricing Page）**：新增 `static/components/views/pricing.html` 作為公開對外的方案定價展示頁，整合於未登入的前台路由系統中。
- **[新增] 三種計費模式選擇**：頁面頂部設計計費切換 Tab，支援「月計費（NT$299/月）」、「年計費（NT$199/月，省 33%）」、「終身授權（NT$5,990，一次付清）」三種方案，點擊 Tab 自動捲動並選取對應方案卡片。
- **[新增] 互動式方案卡片**：三張方案卡片均帶有 Radio Button 選取指示、功能清單對比（月計費 6 項、年計費 8 項、終身 10 項）、最受歡迎 Badge（年計費），支援點擊選取與懸停動畫。
- **[新增] 顧客評語列**：頁面頂部展示三條 5 星顧客評語，呼應參考圖設計。
- **[新增] 功能對照表**：頁面底部新增完整的三方案 10 項功能對照表，年計費欄位以主題色高亮顯示。
- **[新增] 定價頁 CSS 樣式**：`index.css` 新增 `.plan-card`、`.pricing-tab`、`.compare-table`、`.review-item` 等完整定價系統樣式，支援深/淺色主題與 RWD 響應式佈局。
- **[更新] intro.html 導覽列**：在介紹頁導覽列新增「定價」連結，點擊後切換至定價頁面視圖。
- **[更新] app.js**：將 `pricing` 加入 views 動態載入列表。
- **[更新] ui.js**：新增 `pricing: '方案定價'` 頁面標題對應；同步補齊 `smart: '智慧宅控'` 遺漏的標題對應。

### [V42] - 2026-05-27 更新日誌

#### 安全性修補（P0）
- **[安全] 移除所有明碼 Secret**：`config.py` 全面移除 `JWT_SECRET`、`PVE_PASS` 的 hardcode 預設值，改為強制讀取環境變數；`AXIS_JWT_SECRET` 若未設定則服務直接中止，防止以不安全狀態啟動。
- **[安全] 移除 Blynk IoT Token 明碼**：將 `system.py` 中的明碼 Token 移至 `config.py` 集中管理，並改由環境變數 `BLYNK_TOKEN` 提供。
- **[安全] 移除管理員初始密碼明碼**：`utils.py` 的 `init_db_user()` 改由環境變數 `AXIS_ADMIN_PASS` 控制，若未設定則跳過並提示警告。
- **[安全] CORS 收緊**：`allow_origins` 改由環境變數 `ALLOWED_ORIGINS` 控制，生產環境可限定為 `https://absoluteaxis.dpdns.org`，移除 `allow_origins=["*"]` 全開放設定。
- **[安全] 統一管理員角色字串**：前端 `app.js` 中的角色判斷移除不一致的 `role === 'admin'` 分支，統一為 `role === 'Administrator'`，與後端保持一致。
- **[安全] 移除開發機硬編碼路徑**：`config.py` 移除 `/home/sparkle/aiot-master` 路徑偵測邏輯，改為純環境變數 `AXIS_BASE_PATH` 控制。
- **[配置] 更新 `.env.example`**：補齊所有必要環境變數範本（JWT、管理員密碼、PVE、Blynk、CORS、GitHub、Cloudflare 共 10 個）。

#### 架構優化（P1）
- **[架構] 拆分 `/api/system_status` 端點**：將原本合併呼叫 4 個外部 API 的重量級端點，拆分為三個獨立端點：`/api/system/metrics`（每 5 秒）、`/api/system/sensors`（每 30 秒）、`/api/system/github`（每 120 秒）；保留原路由作為向下相容 alias。
- **[架構] 前端輪詢獨立拆分**：`dashboard.js` 的 `startPolling()` 拆分為 `pollMetrics()`、`pollSensors()`、`pollGithub()`、`pollServices()` 四個獨立 setTimeout 鏈，互不阻塞。
- **[架構] 修正 Docker 啟動流程**：`docker-compose.yml` 移除啟動時執行 `pip install` 的問題，依賴安裝在 Dockerfile build 階段完成，容器重啟速度大幅提升。
- **[效能] NAS 上傳改為非同步**：`nas.py` 上傳端點改用 `aiofiles` 非同步串流寫入（每次 1MB 分塊），避免大檔案上傳阻塞 FastAPI event loop。
- **[功能] NAS 分享重複防護**：`/api/nas/share` 加入重複分享判斷，同一 owner/path/target 組合重複分享時回傳 `already_shared` 而非建立重複記錄。

#### 可維護性修補（P2）
- **[維護] 動態版本號注入**：後端 `main.py` 讀取 `APP_VERSION` 環境變數（預設為啟動時間戳），動態替換 `index.html` 中所有靜態資源的 `?v=AXIS_VER` 佔位符，不再需要手動維護版本號。
- **[UI] 統一錯誤處理（Toast 通知系統）**：`api.js` 新增全域 `showToast()` 函式，`authFetch` 統一攔截 403（權限不足）、5xx（伺服器錯誤）、網路斷線並以右下角 Toast 通知呈現。
- **[UI] Toast 元件樣式**：`index.css` 新增 `.toast` / `#toast-container` 樣式，支援 success / error / warning / info 四種類型，含進場動畫與深/淺色主題切換。
- **[依賴] 鎖定 requirements.txt 版本**：鎖定 `paramiko==3.5.1`、`speedtest-cli==2.1.3`，新增 `aiofiles==23.2.1`。

### [V41] - 2026-05-20 更新日誌
- **[新增] 介紹首頁（Landing Page）**：新增 `static/components/views/intro.html` 作為系統的公開對外介紹頁面。頁面採用滿版深色科技風格設計，包含玻璃擬態導覽列、Hero 區塊、五大核心功能模組說明卡片、伺服器安全狀態資訊及頁尾，整體視覺品質大幅提升。
- **[新增] 右上角登入按鈕**：於介紹首頁導覽列右上角新增「系統登入」按鈕，點擊後以 Modal Overlay 形式彈出原有登入框。
- **[新增] 登入框關閉機制**：在原有登入 Overlay 右上角新增關閉按鈕（`×`），並支援點擊背景遮罩關閉，使登入流程更直覺。
- **[重構] 未登入路由分流邏輯**：重構 `app.js` 的 `boot()` 方法，未登入狀態下自動導向介紹首頁（`view-intro`）；已登入狀態則跳過介紹頁，直接進入上次停留的控制台頁面。
- **[新增] 視圖隔離（not-logged-in 模式）**：於 `index.css` 新增 `body.not-logged-in` 樣式覆寫規則，確保介紹首頁顯示時側邊欄與頂部 Header 完全隱藏，實現獨立的全版面佈局。
- **[前端] 新增 `showLoginOverlay()` / `hideLoginOverlay()` 全域方法**：於 `ui.js` 新增登入 Overlay 的控制方法，供介紹首頁的登入按鈕與關閉按鈕呼叫。
- **[前端] 新增 intro 視圖標題對應**：於 `ui.js` 的 `switchView()` 函式新增 `'intro': '服務介紹'` 對應，確保視圖切換標題邏輯完整。
- **[CSS] 介紹首頁響應式樣式**：新增完整的介紹首頁樣式組，包含 `.intro-navbar`、`.intro-hero`、`.intro-features-grid`、`.intro-feature-card`、`.status-box` 等，並支援深色/淺色主題切換與行動裝置 RWD 響應。

### [V40] - 2026-05-17 更新日誌
- **[新增] 🏡 智慧宅控模組**：正式啟用左側導覽列之「🏡 智慧宅控」頁面，作為實體 IoT 與模擬智慧家電的中樞監管平台，全面解除原先的 Placeholder 限制。
- **[新增] 🌡️ {中樞主機環境數據} 專屬區塊**：於智慧宅控頁面劃分專屬環境數據面板，透過 API 介接，動態展現 ESP32 開發板（DHT 感測器）藉由 Blynk IoT 雲端回傳的即時溫度與濕度。
- **[新增] 🧠 環境舒適度與體感指標運算**：實作標準氣象學體感溫度（Feels Like）計算公式，並結合實時溫濕度動態評估環境舒適度（舒適宜人、偏冷、潮濕、偏熱），提供智慧連動之數據基礎。
- **[新增] 💻 實體電腦開機電源控制**：依據實體 ESP32 繼電器點接主機板 PWR 接腳之硬體配置，於前端設計「點擊發送開機脈衝」按鈕，實作物理點接脈衝倒數計時與互動音頻/動畫回饋，防範重複觸發。
- **[新增] 💡 智慧照明與排風控制模擬**：實作「客廳主燈」、「氛圍燈」之模擬照明切換，以及基於環境濕度預警之「低速/強力排風」自動化輔助控制面板。
- **[前端] 模組化組件封裝**：新增獨立視圖元件 `static/components/views/smart.html` 與獨立控制模組 `static/js/smart.js`，並於 `app.js`、`ui.js` 及 `index.html` 完成路由分發註冊與腳本加載，保障程式碼可讀性與維護性。

### [V39] - 2026-05-17 更新日誌
- **[修復] 排除 `system.py` 語法錯誤以解決 502 Bad Gateway**：修復了 `app/routers/system.py` 第 164 行結尾多餘的 `d` 字元，該語法錯誤（SyntaxError）導致 FastAPI 服務啟動失敗與 Web 伺服器 502 閘道器錯誤。

### [V38] - 2026-05-17 更新日誌
- **[修復] FastAPI 啟動崩潰 502 Bad Gateway 排除**：修復了引進多重宇宙後端模組 `minecraft.py` 時，因缺少 `paramiko` 相依套件導致 Docker 容器啟動失敗的 ModuleNotFoundError。
- **[相依性] 更新 `requirements.txt`**：在 `requirements.txt` 中新增 `paramiko` 依賴，確保 Docker 容器在啟動指令引導時自動透過 pip 安裝。
- **[工具] 更新 `pull_and_restart.py`**：將部署指令碼中的容器重啟等待時間從 8 秒延長至 15 秒，以利容器有充足時間自動下載並完成 `paramiko` 安裝，確保 uvicorn 順利就緒。

### [V37] - 2026-05-17 更新日誌
- **[新增] 多重宇宙（Multiverse）Minecraft 管理中樞**：於左側導覽列新增「🌌 多重宇宙」頁面，作為 Minecraft 遊戲伺服器的專屬管理入口，包含伺服器基本資訊、即時線上狀態、連線 IP 資訊與硬體規格顯示。
- **[新增] 管理員 MC 指令控制台**：管理員限定的終端機風格控制台，支援全指令放行模式，透過 SSH + screen stuff 將指令注入 Minecraft 伺服器，並在前端即時顯示指令送出狀態（含稽核日誌記錄）。
- **[新增] 快速指令面板**：管理員快速執行常用 MC 指令（設定時間、天氣、難度、玩家列表）的一鍵按鈕列。
- **[後端] 新增 `app/routers/minecraft.py`**：實作 `GET /api/minecraft/status` 與 `POST /api/minecraft/command` 兩支 API，前者提供詳細 MC 伺服器規格與連線資訊，後者執行 SSH 指令注入並強制管理員身分驗證。
- **[後端] 更新 `app/models.py`**：新增 `MCCommandRequest` Pydantic 模型。
- **[後端] 更新 `app/main.py`**：注冊 `minecraft` router 至主應用程式。
- **[前端] 新增 `static/components/views/multiverse.html`**：多重宇宙頁面視圖元件。
- **[前端] 新增 `static/js/multiverse.js`**：Multiverse 模組邏輯，負責 API 呼叫、DOM 渲染、指令送出與脈搏動畫。
- **[前端] 更新 `index.html`、`app.js`、`ui.js`**：完成側邊欄導覽、動態載入、頁面標題與路由觸發的完整整合。

### [V36] - 2026-05-17 更新日誌
- **[診斷] 外部網路連線中斷排除**：針對 Minecraft 伺服器進行全鏈路網路連線診斷，產出完整外部網路（WAN）連線排除方案與風險評估報告。
- **[工具] 新增網路診斷腳本**：於 `scratch/` 目錄新增 `diag_mc_network.py`，支援自動化透過 SSH 檢測 LXC 容器之埠監聽狀態、閘道器路由、UFW/iptables 防火牆配置與遊戲設定檔參數。
- **[網路] 遷移 Minecraft 內網 IP 服務**：配合使用者外部埠轉發設定，將 Minecraft LXC 容器（ID 102）之內網 IP 自 `192.168.0.108` 變更至 `192.168.0.130`。
- **[工具] 新增 IP 變更自動化指令碼**：於 `scratch/` 新增 `change_mc_ip.py`，支援自動透過 SSH 連結 PVE（`192.168.0.138`）執行 `pct set` 修改容器網路，並調用 `pct reboot` 自動安全重啟。
- **[重構] 更新後端伺服器監測目標**：同步更新 `app/routers/system.py` 之遊戲伺服器連線狀態監控目標 IP 至 `192.168.0.130`。
- **[自動化] 實現 Minecraft 虛擬化雙層自動啟動機制**：配置 Proxmox VE 宿主機開機自動引導 LXC 容器（ID 102），並於容器內部建置 `minecraft.service` 系統守護服務，實現容器開機自動執行背景 screen 與 Java 處理序。
- **[強固] 引入服務防崩潰與安全關檔機制**：systemd 服務模組配置 `Restart=always` 確保崩潰自啟，且支援關機時自動發送 `"stop"` 指令 safe 封存世界存檔，防止地圖資料毀損。
- **[工具] 新增自啟動一鍵配置腳本**：於 `scratch/` 新增 `configure_mc_autostart.py` 自動化 SSH 部署工具。
- **[工具] 新增網路即時狀態監測腳本**：於 `scratch/` 新增 `check_current_lxc_net.py`，支援一鍵分析 PVE 容器網路配置、運行狀態與 Java 連接埠監聽，協助定位外部連線逾時根因。

### [V35] - 2026-05-16 更新日誌
- **[整合] IoT 溫濕度聯動**：介接 Blynk API，將 Absolute-Axis 儀表板首頁的溫度數據，無縫替換為 ESP32 (`power.ino`) DHT 感測器即時回傳的實體房間溫濕度。
- **[新增] 基礎設施自動化引導**：針對 Proxmox PVE (192.168.0.138) 實作自動化啟動配置。
- **[配置] 虛擬機自動啟動 (Autostart)**：將核心虛擬機 `axis-main` (VM ID 100) 設為開機自動啟動 (`onboot: 1`)，確保硬體重啟後服務能秒速回歸。
- **[維護] 服務狀態巡檢**：遠端觸發 VM 100 啟動並確認 Docker 容器 (`axis-server`, `axis-cloudflared`) 具備 `restart: always` 策略，達成全鏈路開機自啟。

### [V34] - 2026-05-02 更新日誌
- **[新增] Proxmox PVE 深度整合**：實作 Proxmox API 介接，現在可在「虛擬化中心」同步讀取宿主機節點狀態（CPU/RAM）以及實體虛擬機（VM/LXC）清單。
- **[優化] 模組化佈局更新**：重構虛擬化中心介面，區分「PVE 實體虛擬機」與「Docker 容器虛擬機」，提供更清晰的基礎設施概覽。
- **[強固] 快取控制機制**：導入全局版本號控制 (V25)，確保組件更新能即時在瀏覽器生效，解決舊版 HTML/JS 殘留問題。

### [V33] - 2026-05-02 更新日誌
- **[修復] 戰情數據精確化**：初始化並掛載 5.5TB 實體硬碟至 NAS 目錄，修正磁碟容量計算異常問題。
- **[優化] 系統監測邏輯**：重構頻寬差值計算公式，提升流量監測之實時性與穩定性。
- **[強化] 狀態反饋機制**：優化 GitHub 監控器之錯誤提示，當 token 缺失時提供引導式訊息而非空白顯示。

### [V32] - 2026-05-02 更新日誌
- **[新增] 硬碟自動識別系統**：重構硬體監測邏輯，移除硬編碼設備路徑，改採 `lsblk` 動態掃描全機磁碟。
- **[強化] 硬體存取權限**：調整 Docker 配置，啟用 `privileged` 模式並掛載 `/dev`，實現容器內對實體硬碟 SMART 狀態與溫度的直接讀取。
- **[優化] 前後端相容性**：修正 API 回傳格式，確保「NAS 管理中樞」之儲存佔用細節能正確呈現動態掃描數據。

### [V31] - 2026-05-02 更新日誌
- **[修復] Cloudflare Tunnel 連線恢復**：修復了 VM 內部 `docker-compose.yml` 遺失 `cloudflared` 服務的問題，並將 Tunnel 管理模式從「本地管理」遷移至「儀表板管理」，成功恢復 `https://absoluteaxis.dpdns.org/` 存取。
- **[部署] Docker 環境強固化**：新增 `Dockerfile` 並修正 `docker-compose.yml` 的網路配置，移除不存在的 `tailscale_mesh` 網路定義，改採穩定的 `host` 模式。
- **[修復] 環境變數對位**：同步 VM 內部的 `.env` 檔案，補齊缺失的 `CLOUDFLARE_TUNNEL_TOKEN`，確保內網穿透服務正常啟動。

### [V30] - 2026-05-02 更新日誌
- **[部署] Cloudflare Tunnel 整合**：將零信任網路通道 (cloudflared) 模組化整合至 `docker-compose.yml`，並統一由 `.env` 檔案管理 Tunnel Token，實現內網穿透一鍵啟動與基礎設施即代碼 (IaC)。

### [V29] - 2026-05-02 更新日誌
- **[部署] IP 權責釐清**：明確標註 PVE 宿主機 (100.124.203.61) 與 VM 虛擬機之連線對象差異，避免開發者連錯主機導致權限問題。

### [V28] - 2026-05-02 更新日誌
- **[修復] 遠端連線逾時診斷**：確認因網路隔離導致無法存取內部 IP `192.168.0.159`，需透過虛擬網格隧道接通。
- **[部署] 網路隧道對接**：引導使用者於 VM 內部完成 Tailscale 部署，實現真正的全域跨網域 SSH 存取。

### [V27] - 2026-05-02 更新日誌
- **[部署] 遠端存取啟用**：完成 Proxmox VM (axis-main) 的 SSH 遠端連線配置指引，確保開發者可從筆電端直接管控。
- **[環境] 階段三初始化**：確立區域網路 (192.168.0.159) 與 Tailscale 虛擬網格雙軌連線方案，解決跨網域存取需求。

### [V26] - 2026-04-30 更新日誌
- **[架構] Proxmox 遷移準備**：完成 Docker Compose 綁定 IP 動態化，將寫死的 Tailscale IP 改為由 `.env` 動態讀取 (`HOST_IP`)，以支援在全新 Proxmox Ubuntu VM 上的自動化部署。
- **[配置] 環境變數分離**：新增 `.env.example` 檔案，確保新主機部署時具備明確的配置參考。

### [V25] - 2026-04-08 更新日誌
- **[架構] 系統遷移準備**：確認採用 Proxmox 虛擬機器 (VM) 方案進行軟體層部署規劃，以獲得最佳 Docker 執行環境。
- **[功能] 模組精簡**：徹底移除全系統之散熱中心與風扇控制模組（包含前端設定面板、前端邏輯 `thermal.js` 以及後端路由 `thermal.py`）。

### [V24] - 2026-04-06 更新日誌
- **[功能] NAS 配額自定義管理**：管理員現在可以針對每個使用者設定不同的 NAS 儲存空間配額（以 GB 為單位），取代原有的全系統 1GB 統一限制。
- **[介面] 管理員面板優化**：在「帳號管理」的建立與編輯視窗新增「儲存空間配額」輸入項，並在列表中即時顯示各成員配額。
- **[架構] 資料庫模型升級**：`User` 模型新增 `quota_bytes` 欄位，實現資料驅動的儲存限制檢查。
- **[基礎整合] Docker 設施橋接 (Phase 2)**：完成 `docker-compose.yml` 模板化建構，並重構後端 `config.py` 以支援環境變數自動化路徑偵測，實現 100% 容器化部署能力。
- **[功能] 私人網路基礎建設 (Phase 1)**：成功部署 Tailscale 網狀網路，建立 NAS (100.83.64.84) 與本地設備 (100.105.220.99) 的加密安全隧道，支援跨網域高速資料傳輸。
- **[修復] 私有雲 (NAS) 服務恢復**：修復了伺服器磁碟 Read-only 引起的 500 錯誤，並加入後端強固化錯誤處理。
- **[優化] 導航持久化 (Persistence)**：現在系統會記憶使用者最後停留的「頁面視圖」與「雲端路徑」，重新整理後不再跳位。
- **[效能] 空間計算優化**：引入目錄大小快取與 `os.scandir` 遞迴，大幅減少 IO 負擔。
- **[功能] 錯誤回報機制**：前端 NAS 列表現在能直接顯示後端具體報錯資訊，提升調試效率。
- **[功能] 上傳後重啟服務**：在雲端選單中新增「上傳並重啟」選項。
- **遞迴優化**：改用 `os.scandir` 取代 `os.walk` 進行目錄掃描，大幅提升深層目錄的大小計算速度。
- **上傳後重啟服務**：在「建立/上傳」選單新增「上傳並重啟服務」選項，支援上傳完成後自動觸發後端重啟。
- **系統重啟 API**：於 `system.py` 新增 `/api/action/restart` 接口，支援安全終止進程以觸發服務管理器自動重啟。
- **UI 同步強化**：上傳監控器現在會在重啟過程中提供明確的進度回饋。

### [V21] - 2026-04-03
#### 【底層重建模擬：絕對佈局解鎖】
- **不再受限**：徹底移除 `display: flex` 容器限制，回歸最具相容性的文件流佈局。
- **固定定位**：將側邊欄固定於 `position: fixed`，主內容區通過 `margin-left` 絕對避讓。
- **捲軸強制啟動**：在 `body` 顯式開啟 `overflow-y: scroll`，將 Windows 與 Chrome 瀏覽器原生的捲動體驗 100% 還原給使用者。
- **UI 操作強化**：捲軸寬度加大至 12px，確保在高解析度屏幕下也能輕鬆點擊。
- **高度適應**：主內容區 (`.main-content`) 自適應內容高度，保證底部的「系統儲存配額」與「發送廣播」在任何情況下都能排隊呈現。
- **空間優化**：增加底部 padding 至 10rem，確保卡片內容在捲動到底時不再被底部邊界視覺壓迫。
- **全域快取更新**：升級至 V21。

### [V20] - 2026-04-03
#### 【核心佈局重構：全局滾動全解鎖】
- **解鎖 Body**：移除視窗高度限制，恢復瀏覽器原生全局滾動，徹底解決窄屏下卡片被切掉的問題。
- **黏性導覽列**：將側邊欄改為 `position: sticky`，確保長頁面滾動時導覽功能始終在左側待命。
- **捲軸回歸**：主捲軸回到視窗最右側，並維持 10px 的主題色自定義顯化樣式。
- **高度適應**：主內容區 (`.main-content`) 自適應內容高度，保證底部的「系統儲存配額」與「發送廣播」在任何情況下都能排隊呈現。
- **空間優化**：增加底部 padding 至 10rem，確保卡片內容在捲動到底時不再被底部邊界視覺壓迫。
- **全域快取更新**：升級至 V20。

### [V19] - 2026-04-03
#### 【視覺強化：主區域滾動條顯現化】
- **UI 同步**：主內容區 (`.main-content`) 自定義 8px 主題色捲軸，與側邊欄風格統一。
- **空間優化**：增加底部 padding 至 10rem，確保卡片內容在捲動到底時不再被底部邊界視覺壓迫。
- **全域快取更新**：升級至 V19。

### [V18] - 2026-04-03
#### 【佈局重構：側邊選單捲軸定案修復】
- **結構手術**：解耦 Logo 區與導覽區，建立 `.sidebar-nav-container` 獨立滾動容器。
- **Flexbox 硬化**：強制讓 Logo 區不縮放 (`flex-shrink: 0`)，徹底消除空間爭奪。
- **捲軸顯化**：將 8px 主題色捲軸精確掛載至內層容器。
- **全域同步**：全面升級 CSS/JS 快取版本號至 V18。

## [2026-04-03] - 系統全資產遷移準備
### **遷移設施 (Migration Infrastructure)**
- **金級鏡像封裝 (Golden Image Packing)**：完成全機代碼、SQLite 資料庫與 NAS 存儲目錄的 1:1 密封打包。
- **自解壓復原腳本 (Bootstrap Script)**：注入 `restore.sh` 以支援全新 Ubuntu 的一鍵依賴安裝與 Tunnel 註冊。
- **跨平台計畫對接**：完成遷移路徑規劃，確保資料零位移與連線無縫。

## [2026-04-02] - 遠端存取前路強固與 UI 機能優化
### **存取配置修正 (Remote Access Optimization)**
- **隧道拓點清洗**：移除所有實驗性子網域 (`app-axis`)，回歸單一主域 `absoluteaxis.dpdns.org`。
- **Ingress 全路徑重置**：精確校準 `cloudflared` 守護行程，移除路徑過濾規則 (`^/blog`)。
- **雙路徑兼容 (Dual-Path Logic)**：同時開啟 Root 與 WWW 子網域映射，修復因習慣輸入 `www` 導致的連線問題。
- **三維稽核診斷**：完成從伺服器內部到外部 TLS 握手的完整診斷，確認隧道聯網能力 100% 正常。

### **UI 與視覺系統優化 (UI & Aesthetics)**
- **頭像同步機制重構**：實作 `app.js` 之中的同步邏輯，確保「導覽列頭像」與「使用者彈窗頭像」即時聯動。
- **CSS 語義化變數標準化**：於 `index.css` 定義並引用 `--success-color`（成功綠）與 `--danger-color`（危險紅），統一全局狀態色值。
- **彈窗 specificity 修復**：修正 `.user-popover` 之 CSS 優先權衝突，確保 `.active` 狀態能正確觸發顯示。
- **組件結構補完**：於 `popover.html` 補齊頭像顯示元素，增強使用者識別度。

### 2026-04-03 實體化接管與 GitHub 監控增強錄
1. **GitHub 即時監控實裝**：
   - 串接 /api/system_status 回傳 /repos/deven951130/Absolute-Axis 即時數據。
   - 前端 5s 輪詢機制與 Pulse 脈搏動畫同步。
2. **NAS 管理中樞實體化 (Bare-Metal Realization)**：
   - 移除 Crucial/WD RED 模擬 Occupants，改為 ST6000DM003 (6TB) 實體數據。
   - 新增 /api/system/hardware 實體硬件掃描接口，整合 smartctl 報告。
   - 重構 RAID 區域為實體儲存池 (Physical Storage Pool)，支援 JBOD 邏輯監控。
3. **系統架構優化**：
   - 建立 axis.service 自動化啟動守護進程，確保後端穩定。
   - 完成 sudoers 特權配置，支援非 root 調用硬體 S.M.A.R.T 指令。

## [2026-04-01] - 實體數據接軌與架構優化
### **核心更新 (Major Architecture Refactor)**
- **實體硬體監控對接 (Hardware Integrity)**：
    - 移除 `system.py` 中所有的 `random` 模擬隨機值。
    - **頻寬真實化**：實作 `NET_STATE` 緩存機制，精確計算物理網卡 MB/s 流量差值。
    - **溫度實例化**：對接 Linux `coretemp` 接口，提供實體與擬合雙重熱能採集。
    - **OS/GPU 動態識別**：基於系統核心路徑，自動回傳真實的伺服器發行版資訊。
- **前端模組化架構精簡**：
    - 完成 `index.html` 徹底拆解，轉向併發式 `app.js` 動態組件加載。

### **機能修正 (Fixes & Aesthetics)**
- **彈窗視覺修復**：修正模組化加載後的視控疊加問題。
- **NAS 選單解鎖**：開通資料夾層級的「直接下載」與「共用」功能，確保權限對位。
- **配色精確命名**：更新品牌配色名為「火花」與「花火」聯名系列。
